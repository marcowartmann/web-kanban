services:
  db:
    image: postgres:18
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-kanban}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-kanban}
      POSTGRES_DB: ${POSTGRES_DB:-kanban}
    volumes:
      # postgres:18+ stores data in a version subdir; mount the parent dir
      # (mounting /var/lib/postgresql/data fails on 18+).
      - pgdata:/var/lib/postgresql
    # Expose Postgres to external clients (psql/DBeaver/pgAdmin). Bound to
    # 127.0.0.1 by default so only this host can reach it; set DB_BIND=0.0.0.0
    # in .env to allow LAN access (pair with a strong POSTGRES_PASSWORD).
    ports:
      - "${DB_BIND:-127.0.0.1}:${DB_PORT:-5432}:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-kanban}"]
      interval: 5s
      timeout: 3s
      retries: 10

  backend:
    build: ./backend
    environment:
      DATABASE_URL: postgresql+psycopg://${POSTGRES_USER:-kanban}:${POSTGRES_PASSWORD:-kanban}@db:5432/${POSTGRES_DB:-kanban}
      BOOTSTRAP_ADMIN: "true"
      # Encrypts the stored SFTP backup password. Change in production.
      APP_SECRET: ${APP_SECRET:-dev-insecure-change-me}
      # Set to "true" behind TLS so the session cookie is HTTPS-only.
      COOKIE_SECURE: ${COOKIE_SECURE:-false}
      # Dev defaults — override all three in any real deployment.
      INITIAL_ADMIN_EMAIL: ${INITIAL_ADMIN_EMAIL:-admin@example.com}
      INITIAL_ADMIN_PASSWORD: ${INITIAL_ADMIN_PASSWORD:-admin}
      INITIAL_ADMIN_NAME: ${INITIAL_ADMIN_NAME:-Admin}
      INITIAL_ADMIN_USERNAME: ${INITIAL_ADMIN_USERNAME:-admin}
      # LDAP authentication — off by default; set LDAP_ENABLED=true + the rest to enable.
      LDAP_ENABLED: ${LDAP_ENABLED:-false}
      LDAP_SERVER_URI: ${LDAP_SERVER_URI:-ldaps://ldap.internal:636}
      LDAP_START_TLS: ${LDAP_START_TLS:-false}
      LDAP_CA_CERT_FILE: ${LDAP_CA_CERT_FILE:-}
      LDAP_BIND_DN: ${LDAP_BIND_DN:-}
      LDAP_BIND_PASSWORD: ${LDAP_BIND_PASSWORD:-}
      LDAP_BASE_DN: ${LDAP_BASE_DN:-ou=people,dc=example,dc=com}
      LDAP_USER_FILTER: ${LDAP_USER_FILTER:-(&(objectClass=inetOrgPerson)(uid={uid}))}
      LDAP_ATTR_EMAIL: ${LDAP_ATTR_EMAIL:-mail}
      LDAP_ATTR_DISPLAY_NAME: ${LDAP_ATTR_DISPLAY_NAME:-cn}
    volumes:
      - snapshots:/app/snapshots
    depends_on:
      db:
        condition: service_healthy
    ports:
      - "${BACKEND_PORT:-8000}:8000"
    healthcheck:
      # 127.0.0.1 (not localhost): the probe runs inside this container and
      # localhost may resolve to IPv6 ::1, where uvicorn (bound to IPv4 0.0.0.0)
      # isn't listening. start_period keeps early boot failures from counting.
      test: ["CMD", "python", "-c", "import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/api/health', timeout=3).status == 200 else 1)"]
      interval: 5s
      timeout: 5s
      retries: 10
      start_period: 30s

  frontend:
    build:
      context: ./frontend
      # FontAwesome Pro token for `npm ci`, passed as a BuildKit secret sourced
      # from the FONTAWESOME_PACKAGE_TOKEN env var. Never written to a layer.
      secrets:
        - fa_token
    depends_on:
      backend:
        condition: service_healthy
    volumes:
      # TLS cert/key live on the host. If ./nginx/certs is empty on first start,
      # a self-signed dev cert (server.crt/server.key) is generated into it;
      # replace those with real files to use your own certificate.
      - ./nginx/certs:/etc/nginx/certs
    ports:
      - "${APP_PORT:-8080}:80"
      - "${HTTPS_PORT:-8443}:443"

  # Dev SFTP target for backups (atmoz/sftp). The backend reaches it at sftp:22.
  sftp:
    image: atmoz/sftp:alpine
    command: ["${SFTP_USER:-kanban}:${SFTP_PASSWORD:-kanban}:::upload"]
    volumes:
      # backups land here on the host: ./sftp/uploads
      - ./sftp/uploads:/home/${SFTP_USER:-kanban}/upload
      # runs as root at boot to make the mounted upload volume writable
      - ./sftp/chown-upload.sh:/etc/sftp.d/chown-upload.sh:ro
    ports:
      - "127.0.0.1:${SFTP_PORT:-2222}:22"

secrets:
  fa_token:
    environment: FONTAWESOME_PACKAGE_TOKEN

volumes:
  pgdata:
  snapshots:
